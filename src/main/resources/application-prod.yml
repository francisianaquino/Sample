spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:mysql://localhost:3306/virtual_tour?useSSL=false&Timezone=true&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true